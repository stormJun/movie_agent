# backend/infrastructure/persistence

Persistence implementations owned by the service side (not by `graphrag_agent` core).

- `postgres/`: Postgres-backed stores for conversations, feedback, and related system records.

