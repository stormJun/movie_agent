# Dev proxy (Vite) will forward /api -> VITE_BACKEND_ORIGIN
VITE_BACKEND_ORIGIN=http://localhost:8324

# If you want to bypass proxy, set:
# VITE_API_BASE_URL=http://localhost:8324

# For this repo's default backend port:
# VITE_BACKEND_ORIGIN=http://localhost:8324
